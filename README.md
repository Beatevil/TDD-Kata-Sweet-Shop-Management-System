# 🍭 Sweet Shop Management System

> **TDD Kata | Full‑Stack Project**
> A clean, modern Sweet Shop Management System built using **Test‑Driven Development**, secure APIs, and a beautiful SPA frontend.

---

## 🎯 Objective

Build and test a full‑stack application to manage sweets, inventory, and users while following **TDD**, clean code, and modern Git workflows.

---

## 🧠 Tech Stack

**Backend**

* Node.js (TypeScript) / Python / Java / Ruby
* RESTful API + JWT Authentication
* Database: PostgreSQL / MongoDB / SQLite

**Frontend**

* React / Vue / Angular / Svelte
* Responsive Single‑Page Application

---

## 🔐 Key Features

* User registration & login (JWT)
* Role‑based access (User / Admin)
* Manage sweets (add, update, delete)
* Purchase & restock inventory
* Search sweets by name, category, or price

---

## 📦 Core API Endpoints

* `POST /api/auth/register`
* `POST /api/auth/login`
* `GET /api/sweets`
* `POST /api/sweets`
* `PUT /api/sweets/:id`
* `DELETE /api/sweets/:id` *(Admin)*
* `POST /api/sweets/:id/purchase`
* `POST /api/sweets/:id/restock` *(Admin)*

---

## 🧪 Test‑Driven Development

* Tests written **before** implementation
* Clear **Red → Green → Refactor** workflow
* High test coverage for backend logic & APIs

---

## 🤖 My AI Usage

AI tools were used **responsibly and transparently**.

* Used for boilerplate, tests, debugging & refactoring
* AI added as **co‑author** in relevant commits:

```
```

---

✨ *Tests first. Clean code always. Built to scale.*

  
